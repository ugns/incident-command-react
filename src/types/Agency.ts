// Agency model/type for use across the app

export interface Agency {
  agencyId: string;
  org_id: string;
  name: string;
  contactPerson: string;
  address: string;
  phone: string;
  // Add more fields as needed
}
